# calculate the sin and cos of the angles in the given array
import numpy as np  
a = np.array([0, 30,45, 60, 90])#these angles are considered in radians
print(np.sin(a)) # prints the sin of the angles
print(np.cos(a)) #prints the cos of angles in the array